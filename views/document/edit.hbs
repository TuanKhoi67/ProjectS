
<head>
    <title>Edit Document</title>
</head>
<body>
    <div class="container mt-4">
        <h2 class="text-center mb-4">Edit Document</h2>
        <form action="/document/update/{{document._id}}" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="title">Title</label>
                <input type="text" class="form-control" id="title" name="title" value="{{document.title}}" required>
            </div>
            <div class="mb-3">
                <label for="content">Content</label>
                <textarea class="form-control" id="content" name="content" rows="5" required>{{document.content}}</textarea>
            </div>
            <div class="mb-3">
                <label for="imageUrl">Image URL</label>
                <input type="text" class="form-control" id="imageUrl" name="imageUrl" value="{{document.imageUrl}}" required>
            </div>
            <div class="mb-3">
                <label for="documentFile">Upload Document (Word/PDF)</label>
                <input type="file" class="form-control" id="documentFile" name="documentFile" accept=".docx, .pdf">
            </div>
            <button type="submit" class="btn btn-primary">Save Changes</button>
        </form>
    </div>
</body>