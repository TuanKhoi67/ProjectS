<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Chỉnh sửa người dùng</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <h2>Edit user information</h2>

    <form action="/userpage/edit/{{user._id}}" method="POST">
      <div class="mb-3">
        <h4 for="fullname" >Name:</h4>
        <input type="text" class="form-control" id="fullname" name="fullname" value="{{user.fullname}}" required>
      </div>

      <div class="mb-3">
        <h4 for="email" >Email</h4>
        <input type="email" class="form-control" id="email" name="email" value="{{user.email}}" required>
      </div>

      <div class="mb-3">
        <h4 for="role" >Role</h4>
        <select class="form-select" id="role" name="role" required>
          <option value="student" {{#if (eq user.role 'student')}}selected{{/if}}>Student</option>
          <option value="tutor" {{#if (eq user.role 'tutor')}}selected{{/if}}>Tutor</option>
          <option value="admin" {{#if (eq user.role 'admin')}}selected{{/if}}>Admin</option>
        </select>
      </div>

      <div class="mb-3">
        <h4 for="password" >New password (leave blank if not changed)</label>
        <input type="password" class="form-control" id="password" name="password">
      </div>

      <button type="submit" class="btn btn-primary">Save</button>
      <a href="/userpage/admin" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>

  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>